# ControlAR CSGO Seen-10 isolated runtime. Install with scripts/setup_csgo_seen10.sh.
# Torch/Torchvision must use CUDA >= 12.8. The setup script verifies this, then
# copies the already validated CUDA 12.8 environment when available (or installs
# the stable cu128 wheels into a new isolated venv).
--extra-index-url https://download.pytorch.org/whl/cu128

torch>=2.7.1
torchvision>=0.22.1
numpy==1.26.4
Pillow==10.4.0
einops==0.8.1
transformers==4.57.3
accelerate==1.12.0
safetensors==0.5.3
huggingface_hub==0.36.0
timm==1.0.24
opencv-python-headless==4.11.0.86
requests==2.32.3
